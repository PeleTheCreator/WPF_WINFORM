<UserControl x:Class="CustomUserControl.MyCustomTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomUserControl"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="250">
    <Grid Margin="0,0,0,10" >
        <TextBox x:Name="inputTxt" VerticalContentAlignment="Center" FontSize="16" FontWeight="Light" TextChanged="inputTxt_TextChanged"/>
        <TextBlock x:Name="placeholderTxt" Text="{Binding Placeholder}"  Foreground="DarkGray"  Margin="5,0,0,0"  VerticalAlignment="Center" 
            FontSize="16" FontWeight="Light" IsHitTestVisible="False" />
        <Button x:Name="clearBtn" Content="X" Width="30" Background="Transparent" HorizontalAlignment="Right"  Foreground="LightGray"
               BorderThickness="0" Click="clearBtn_Click"/>
    </Grid>
</UserControl>
