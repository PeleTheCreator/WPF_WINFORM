<UserControl x:Class="CustomUserControl.MyCustomUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomUserControl"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="200">
    <Border CornerRadius="8" Background="LightBlue" Padding ="10">
        <StackPanel>
            <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=UserControl}}" FontSize="16" FontWeight="Bold"/>
            <Button Content="Click Me" Margin="0, 10, 0, 0" Click="Button_Click"></Button>
        </StackPanel>
    </Border>
</UserControl>
