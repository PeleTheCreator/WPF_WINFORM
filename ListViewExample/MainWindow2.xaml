<Window x:Class="ListViewExample.MainWindow2"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ListViewExample"
        mc:Ignorable="d"
        Title="MainWindow2" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="20"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <TextBox x:Name="inputTxt" Grid.Row="1" Height="30" Margin="100,0,0,0"/>

        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal">
            <Button x:Name="AddBtn" Width="100" Margin="0,0,10,0" Content="Add" Click="AddBtn_Click"/>
            <Button x:Name="RemoveBtn" Width="100" Margin="0,0,10,0" Content="Remove" Click="RemoveBtn_Click"/>
            <Button x:Name="ClearBtn" Width="100"  Content="Clear" Click="ClearBtn_Click"/>
        </StackPanel>

        <ListView x:Name="displayLV" Grid.Row="2" Grid.ColumnSpan="2" Margin="40" ItemsSource="{Binding Entries}" SelectionMode="Multiple"/>
    </Grid>
</Window>
