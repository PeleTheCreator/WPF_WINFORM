<Window x:Class="MVVMExample.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVVMExample"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="500">
    <Grid>
  
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Add" Width="75" Margin="10" Command="{Binding AddCommand}"/>
            <Button Content="Delete" Width="75" Margin="10" Command="{Binding DeleteCommand}"/>
            <Button Content="Save" Width="75" Margin="10" Command="{Binding SaveCommand}"/>
        </StackPanel>
        <Grid Grid.Row="1" Grid.ColumnSpan="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="7*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <DataGrid
                ItemsSource="{Binding Items}"
                SelectedItem="{Binding SeletectedItem}"
                CanUserAddRows="False"
                AutoGenerateColumns="False">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*"/>
                    <DataGridTextColumn Header="SerialNumber" Binding="{Binding SerialNumber}" Width="*"/>
                    <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="*"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Grid.Column="1">
                <StackPanel.Resources>
                    <Style TargetType="TextBox">
                        <Setter Property="Width" Value="150"/>
                        <Setter Property="Height" Value="25"/>
                        <Setter Property="Margin" Value="5,0,5,15"/>
                    </Style>
                </StackPanel.Resources>
                <Label Content="Name"/>
                <TextBox Text="{Binding SeletectedItem.Name, UpdateSourceTrigger=PropertyChanged}"/>

                <Label Content="Serial Number"/>
                <TextBox Text="{Binding SeletectedItem.SerialNumber, UpdateSourceTrigger=PropertyChanged}"/>

                <Label Content="Quality"/>
                <TextBox Text="{Binding SeletectedItem.Quantity, UpdateSourceTrigger=PropertyChanged}"/>
                
                
            </StackPanel>
        </Grid>


    </Grid>
</Window>
