<Window x:Class="CustomWindowControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CustomWindowControl"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowStyle="None" MouseLeftButtonDown="Window_MouseLeftButtonDown">
        <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0" GlassFrameThickness="0" CornerRadius="0"/>
        </WindowChrome.WindowChrome>


        <Grid Background="DarkOliveGreen">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right"  Grid.Row="0">
                <Button x:Name="MinimizeButton" BorderThickness="0" Content="--" Width="40" Height="40" Click="MinimizeButton_Click" Background="Transparent" FontSize="15"  Foreground="White" FontWeight="Bold" />
                <Button x:Name="MaximizeButton" BorderThickness="0" Content="[]" Width="40" Height="40"  Click="MaximizeButton_Click" Background="Transparent" FontSize="15"  Foreground="White" FontWeight="Bold"/>
                <Button x:Name="CloseButton" BorderThickness="0"  Content="X" Width="40" Height="40" Click="CloseButton_Click" Background="Transparent" Foreground="White" FontSize="15" FontWeight="Bold"/>
            </StackPanel>
        </Grid>
</Window>
